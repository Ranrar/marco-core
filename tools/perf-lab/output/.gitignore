# Ignore all generated benchmark artifacts
# (timestamped JSON, CSV, Markdown run outputs)
*.json
*.csv
*.md

# Keep sentinel files and the baseline manifest
!.gitkeep
!baseline/workload-manifest-v1.json